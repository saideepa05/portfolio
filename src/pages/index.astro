---
import { getCollection } from 'astro:content';
import BlogPreview from '../components/BlogPreview.astro';
import CallToAction from '../components/CallToAction.astro';
import ContactCTA from '../components/ContactCTA.astro';
import Grid from '../components/Grid.astro';
import Hero from '../components/Hero.astro';
import Icon from '../components/Icon.astro';
import Pill from '../components/Pill.astro';
import PortfolioPreview from '../components/PortfolioPreview.astro';
import BaseLayout from '../layouts/BaseLayout.astro';

const recentPosts = (await getCollection('blog'))
	.sort((a, b) => b.data.publishDate.valueOf() - a.data.publishDate.valueOf())
	.slice(0, 3);

const projects = (await getCollection('work')).sort(
	(a, b) => b.data.publishDate.valueOf() - a.data.publishDate.valueOf(),
);

const skills = [
	{
		group: 'AI Systems & Frameworks',
		items: [
			'LLMs, RAG Pipelines, Agentic AI',
			'Multi-Agent Systems',
			'LangChain, LlamaIndex, Hugging Face',
			'Vector Databases (FAISS, Pinecone)',
			'OpenAI API, Embedding Models',
			'Prompt Engineering',
		],
	},
	{
		group: 'Neuromorphic & RL Research',
		items: [
			'Reinforcement Learning',
			'Spiking Neural Networks (snnTorch, PyTorch)',
			'Deep Reinforcement Learning',
			'Surrogate Gradient Learning',
			'Transformers, CNNs, RNNs, LSTMs',
			'Spike Encodings & Energy Trade-offs',
		],
	},
	{
		group: 'Engineering & Deployment',
		items: [
			'Python, Java, SQL, C++',
			'FastAPI, REST APIs, Spring Boot',
			'Docker, CI/CD, GitHub Actions',
			'AWS',
			'ETL Pipelines',
		],
	},
];

const publications = [
	{
		venue: 'IEEE Access · 2026',
		title: 'Exploring Neuromorphic Computing for Reinforcement Learning: A Survey and Review',
		href: 'https://ieeexplore.ieee.org/abstract/document/11353437',
	},
	{
		venue: 'IEEE Conference on Games (CoG) · 2025',
		title: 'RL- and LLM-Based AI Solvers for Wordle and Fibble',
		href: 'https://ieeexplore.ieee.org/abstract/document/11114257',
	},
];
---

<BaseLayout>
	<div class="stack gap-20 lg:gap-20">
		<div class="wrapper stack gap-8 lg:gap-20">
			<header class="hero">
				<Hero
					title="Hi, I'm Sai Deepa Kadaru"
					tagline="AI/ML Engineer with strong expertise in building intelligent systems using machine learning, deep learning, and reinforcement learning techniques."
					align="start"
				>
					<div class="roles">
						<Pill><Icon icon="code" size="1.33em" /> AI/ML Engineer</Pill>
						<Pill><Icon icon="pencil-line" size="1.33em" /> Researcher</Pill>
						<Pill><Icon icon="trophy" size="1.33em" /> Published Author</Pill>
					</div>
				</Hero>

				<img
					alt="Sai Deepa Kadaru profile photo"
					width="220"
					height="220"
					src="/assets/portrait.jpg"
				/>
			</header>
		</div>

		<main class="wrapper stack gap-20 lg:gap-28">

			<section class="section section-split with-background">
				<header class="section-header stack gap-2 lg:gap-4">
					<h3>My Projects</h3>
					<p>Production AI systems, research projects, and published work — each built to solve a real problem.</p>
				</header>

				<div class="gallery">
					<div class="projects-grid">
						{projects.map((project) => (
							<PortfolioPreview project={project} />
						))}
					</div>
				</div>
			</section>

			<section class="section section-split with-background bg-variant">
				<header class="section-header stack gap-2 lg:gap-4">
					<h3>Publications</h3>
					<p>
						Peer-reviewed research in neuromorphic computing, reinforcement learning, and LLM-based game solvers.
					</p>
				</header>

				<div class="gallery">
					<Grid variant="small">
						{
							publications.map((pub) => (
								<li class="mention-card">
									{pub.href ? (
										<a href={pub.href} target="_blank" rel="noopener noreferrer" class="pub-link">
											<p class="pub-venue">{pub.venue}</p>
											<p class="pub-title">{pub.title}</p>
										</a>
									) : (
										<div class="pub-link">
											<p class="pub-venue">{pub.venue}</p>
											<p class="pub-title">{pub.title}</p>
										</div>
									)}
								</li>
							))
						}
					</Grid>
				</div>
			</section>

			<div class="about-wrapper">
				<section class="about-section">
					<h2 class="about-section-title">Background</h2>
					<div class="about-content">
						<p>
							I recently completed my MS in Computer Science at Ohio University, where my thesis focuses
							on neuromorphic computing and spiking neural networks with the intersection of reinforcement
							learning, where I explore energy-efficient alternatives to traditional deep RL using
							biologically inspired hardware.
						</p>
						<p>
							On the engineering side, I build production-grade AI systems: RAG pipelines,
							multi-agent orchestrators, LLM monitoring stacks, and backend APIs that actually ship.
							I care deeply about reliability, clarity, and systems that non-technical collaborators
							can operate without hand-holding.
						</p>
					</div>
				</section>

				<section class="about-section">
					<h2 class="about-section-title">Education</h2>
					<div class="about-content">
						<div class="edu-entry">
							<div class="edu-header">
								<strong>Ohio University</strong>&nbsp;· Athens, OH
								<span class="edu-dates">Aug 2023 – Dec 2026</span>
							</div>
							<p class="edu-degree">Master of Science, Computer Science &nbsp;·&nbsp; GPA: 3.7 / 4.0</p>
						</div>
						<div class="edu-entry">
							<div class="edu-header">
								<strong>Osmania University</strong>&nbsp;· Hyderabad, India
								<span class="edu-dates">Aug 2019 – Jul 2023</span>
							</div>
							<p class="edu-degree">Bachelor of Engineering, Computer Science</p>
						</div>
					</div>
				</section>

				<section class="about-section">
					<h2 class="about-section-title">Experience</h2>
					<div class="about-content">
						<div class="edu-entry">
							<div class="edu-header">
								<strong>Graduate Research Assistant</strong>&nbsp;· Ohio University
								<span class="edu-dates">Aug 2023 – Aug 2026</span>
							</div>
							<ul>
								<li>Designed &amp; deployed end-to-end AI systems using LLMs, RAG pipelines, and agent frameworks on AWS.</li>
								<li>Built and integrated AI systems with existing data infrastructure via REST APIs and microservices.</li>
								<li>Debugged and optimized production AI pipelines, identifying failure modes and tuning retrievals.</li>
								<li>Wrote modular, reusable Python code across LLM integration, vector search, and agent orchestration layers with clear documentation.</li>
							</ul>
						</div>
						<div class="edu-entry">
							<div class="edu-header">
								<strong>Programmer Analyst Intern</strong>&nbsp;· Cognizant
								<span class="edu-dates">Feb 2023 – Jul 2023</span>
							</div>
							<ul>
								<li>Automated backend QA workflows using Selenium and Java for enterprise clients, delivering production-ready automation that reduced manual testing effort measurably.</li>
								<li>Documented testing methodologies, failure patterns, and resolution strategies for both client and internal teams.</li>
							</ul>
						</div>
					</div>
				</section>

				<section class="about-section">
					<h2 class="about-section-title">Skills</h2>
					<div class="about-content">
						<div class="skills-grid">
							{skills.map((group) => (
								<div class="skill-group">
									<h3 class="skill-group-title">{group.group}</h3>
									<div class="skill-pills">
										{group.items.map((item) => (
											<span class="skill-pill">{item}</span>
										))}
									</div>
								</div>
							))}
						</div>
					</div>
				</section>
			</div>

			<section class="section section-split with-background blog-section">
				<header class="section-header stack gap-2 lg:gap-4">
					<h3>Blog Posts</h3>
					<p>Thoughts on AI engineering, neuromorphic computing, and building things that work in production.</p>
				</header>

				<div class="gallery">
					<div class="blog-grid">
						{recentPosts.map((post) => (
							<BlogPreview post={post} />
						))}
					</div>
				</div>
			</section>

			<div class="about-wrapper">
				<section class="about-section">
					<h2 class="about-section-title">Contact</h2>
					<div class="about-content">
						<div class="contact-links">
							<a href="https://github.com/saideepa05" target="_blank" rel="noopener noreferrer">GitHub</a>
							<a href="https://www.linkedin.com/in/sai-deepa/" target="_blank" rel="noopener noreferrer">LinkedIn</a>
							<a href="mailto:saideepa0501@gmail.com">saideepa0501@gmail.com</a>
						</div>
					</div>
				</section>
			</div>

		</main>

		<div class="cta-wrapper">
			<ContactCTA />
		</div>
	</div>
</BaseLayout>

<style>
	/* ====================================================== */
	/* Hero */

	.hero {
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 2rem;
	}

	.roles {
		display: none;
	}

	.hero img {
		aspect-ratio: 1 / 1;
		object-fit: cover;
		object-position: top center;
		border-radius: 50%;
		box-shadow: var(--shadow-md);
		width: 180px;
		height: 180px;
	}

	@media (min-width: 50em) {
		.hero {
			display: grid;
			grid-template-columns: 7fr 3fr;
			padding-inline: 2.5rem;
			gap: 2rem;
		}

		.roles {
			margin-top: 0.5rem;
			display: flex;
			gap: 0.5rem;
		}

		.hero img {
			aspect-ratio: 1 / 1;
			width: 200px;
			height: 200px;
			border-radius: 50%;
			object-fit: cover;
			object-position: top center;
		}
	}

	/* ====================================================== */
	/* About-style two-column sections */

	.about-wrapper {
		display: flex;
		flex-direction: column;
		gap: 1.5rem;
		color: var(--gray-200);
	}

	.about-section {
		display: flex;
		flex-direction: column;
		gap: 0.3rem;
	}

	.about-section-title {
		font-size: var(--text-md);
		color: var(--gray-0);
	}

	.about-content p {
		margin: 0 0 0.4rem;
	}

	.about-content a {
		color: var(--accent-regular);
		text-decoration: 1px solid underline transparent;
		text-underline-offset: 0.25em;
		transition: text-decoration-color var(--theme-transition);
	}

	.about-content a:hover {
		text-decoration-color: currentColor;
	}

	@media (min-width: 50em) {
		.about-section {
			display: grid;
			grid-template-columns: 1fr 60% 1fr;
			align-items: start;
			font-size: 0.8rem;
		}

		.about-section-title {
			grid-column: 1;
			padding-top: 0.15rem;
		}

		.about-content {
			grid-column: 2 / 4;
		}
	}

	/* Education & Experience */
	.edu-entry {
		margin-bottom: 0.75rem;
	}

	.edu-entry:last-child {
		margin-bottom: 0;
	}

	.edu-header {
		display: flex;
		flex-wrap: wrap;
		gap: 0.25rem 0.5rem;
		align-items: baseline;
		color: var(--gray-0);
		margin-bottom: 0.25rem;
	}

	.edu-dates {
		margin-left: auto;
		color: var(--gray-400);
		white-space: nowrap;
	}

	.edu-degree {
		margin: 0 0 0.4rem;
		color: var(--gray-300);
	}

	.edu-detail {
		margin: 0 0 0.3rem;
		color: var(--gray-300);
		line-height: 1.6;
	}

	.edu-entry ul {
		margin: 0.2rem 0 0 1rem;
		padding: 0;
		color: var(--gray-300);
		line-height: 1.5;
	}

	/* Skills */
	.skills-grid {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 1rem;
	}

	.skill-group {
		background: var(--gradient-subtle);
		border: 1px solid var(--gray-800);
		border-radius: 1rem;
		padding: 1.5rem 1.75rem;
	}

	.skill-group-title {
		font-size: 0.75rem;
		font-weight: 700;
		color: var(--accent-regular);
		text-transform: uppercase;
		letter-spacing: 0.06em;
		margin: 0 0 0.9rem;
	}

	.skill-pills {
		display: flex;
		flex-wrap: wrap;
		gap: 0.5rem;
	}

	.skill-pill {
		display: inline-flex;
		align-items: center;
		padding: 0.35rem 0.85rem;
		border-radius: 999rem;
		background-color: var(--accent-regular);
		color: var(--accent-text-over);
		font-size: var(--text-sm);
		font-weight: 500;
		line-height: 1.2;
	}

	/* Contact */
	.contact-links {
		display: flex;
		flex-wrap: wrap;
		gap: 1.25rem;
		align-items: center;
		font-size: var(--text-md);
		color: var(--gray-300);
	}

	@media (max-width: 49.9em) {
		.skills-grid {
			grid-template-columns: 1fr;
		}

		.edu-dates {
			margin-left: 0;
			width: 100%;
		}
	}

	/* ====================================================== */
	/* CTA wrapper — pulls the CTA closer to the last section */

	.cta-wrapper {
		margin-top: -3rem;
	}

	/* ====================================================== */
	/* Styled sections (Projects, Publications, Blog) */

	.section {
		display: grid;
		gap: 2rem;
	}

	.with-background {
		position: relative;
	}

	.with-background::before {
		--hero-bg: var(--bg-image-subtle-2);

		content: '';
		position: absolute;
		pointer-events: none;
		left: 50%;
		width: 100vw;
		aspect-ratio: calc(2.25 / var(--bg-scale));
		top: 0;
		transform: translateY(-75%) translateX(-50%);
		background:
			url('/assets/backgrounds/noise.png') top center/220px repeat,
			var(--hero-bg) center center / var(--bg-gradient-size) no-repeat,
			var(--gray-999);
		background-blend-mode: overlay, normal, normal, normal;
		mix-blend-mode: var(--bg-blend-mode);
		z-index: -1;
	}

	.with-background.bg-variant::before {
		--hero-bg: var(--bg-image-subtle-1);
	}

	.section-header {
		justify-self: center;
		text-align: center;
		max-width: 50ch;
		font-size: var(--text-md);
		color: var(--gray-300);
	}

	.section-header h3 {
		font-size: var(--text-2xl);
	}

	@media (min-width: 50em) {
		.section {
			grid-template-columns: repeat(4, 1fr);
			grid-template-areas: 'header header header header' 'gallery gallery gallery gallery';
			gap: 5rem;
		}

		.section-header {
			grid-area: header;
			font-size: var(--text-lg);
		}

		.section-header h3 {
			font-size: var(--text-4xl);
		}

		.gallery {
			grid-area: gallery;
		}

		.centered-cta {
			display: flex;
			justify-content: center;
			align-items: center;
		}

		/* Split layout: heading left, content right — matches about-section style */
		.section.section-split {
			grid-template-columns: 1fr 3fr;
			grid-template-areas: 'header gallery';
			gap: 2rem;
			align-items: start;
		}

		.section.section-split .section-header {
			grid-area: header;
			text-align: left;
			max-width: none;
			justify-self: start;
		}

		.section.section-split .section-header h3 {
			font-size: var(--text-xl);
			color: var(--gray-0);
		}

		.section.section-split .section-header p {
			font-size: var(--text-sm);
			color: var(--gray-400);
			margin-top: 0.4rem;
		}

		.section.section-split .gallery {
			grid-area: gallery;
		}
	}

	/* ====================================================== */
	/* Projects grid */

	.projects-grid {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 1.5rem;
		width: 100%;
	}

	@media (min-width: 50em) {
		.projects-grid {
			grid-template-columns: repeat(3, 1fr);
		}
	}

	/* ====================================================== */
	/* Publications cards */

	.mention-card {
		display: flex;
		min-height: 7rem;
		justify-content: center;
		align-items: stretch;
		text-align: left;
		border: 1px solid var(--gray-800);
		border-radius: 1.5rem;
		color: var(--gray-300);
		background: var(--gradient-subtle);
		box-shadow: var(--shadow-sm);
		overflow: hidden;
	}

	.pub-link {
		display: flex;
		flex-direction: column;
		gap: 0.4rem;
		padding: 1.25rem 1.5rem;
		text-decoration: none;
		color: inherit;
		width: 100%;
		transition: background-color var(--theme-transition);
	}

	a.pub-link:hover {
		background-color: var(--accent-subtle-overlay);
	}

	.pub-venue {
		font-size: 0.8rem;
		font-weight: 700;
		text-transform: uppercase;
		letter-spacing: 0.05em;
		color: var(--accent-regular);
		margin: 0;
	}

	.pub-title {
		font-size: var(--text-md);
		line-height: 1.5;
		margin: 0;
	}

	@media (min-width: 50em) {
		.mention-card {
			border-radius: 1.5rem;
			min-height: 11rem;
		}
	}

	/* ====================================================== */
	/* Blog section */

	.blog-grid {
		display: grid;
		gap: 1rem;
		grid-template-columns: 1fr;
	}

	@media (min-width: 50em) {
		.blog-grid {
			grid-template-columns: repeat(3, 1fr);
			gap: 1.5rem;
		}
	}
</style>
